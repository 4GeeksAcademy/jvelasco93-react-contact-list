export const getInitialState = () => {
  return {};
}

export default function reducer(state, action = {}) {
  switch (action.type) {
    // 


    default:
      throw Error('Unknown action.');
  }
}
